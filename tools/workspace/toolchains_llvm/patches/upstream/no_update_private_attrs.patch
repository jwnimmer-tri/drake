[toolchains_llvm] Silence "canonical reproducible form" warning

Submitted upstream here:
https://github.com/bazel-contrib/toolchains_llvm/pull/468

--- toolchain/internal/common.bzl
+++ toolchain/internal/common.bzl
@@ -226,6 +226,9 @@
     for key in dir(attr):
         if not hasattr(attr, key):
             fail("key %s not found in attributes" % key)
+        if key[0] == "_":
+            # Don't update private attrs.
+            continue
         val = getattr(attr, key)
 
         # Make mutable copies of frozen types.
